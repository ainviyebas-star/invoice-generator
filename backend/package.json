{
  "name": "invoice-backend",
  "version": "1.0.0",
  "description": "Cloudflare Worker with D1 for invoice management",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute invoice-db --file=./src/schema.sql"
  },
  "dependencies": {
    "itty-router": "^4.2.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "wrangler": "^4.80.0"
  }
}
